<table id="example2" class="table table-bordered table-hover">
    <thead>
        <tr>
            <th>No.</th>
            <th>Nama</th>
            <th>Jumlah</th>
            <th>Harga</th>
            <th class="text-center">Action</th>
        </tr>
    </thead>
    <tbody> 
     
    <% for( let i = 0; i <= nominal.length; i++ ) { %>
        <% if (nominal[i]?.coinName) { %>
            <tr>
                <td><%= i + 1 %> </td>
                <td><%= nominal[i]?.coinName %> </td> 
                <td><%= nominal[i]?.coinQuantity %> </td> 
                <td><%= nominal[i]?.price %> </td> 
                <td class="text-center">
                <form action="/nominal/delete/<%= nominal[i]?._id %>?_method=DELETE " method="post">
                    <a href="/nominal/edit/<%= nominal[i]?._id %> " class="btn btn-warning" style="color: white;">
                    <i class="fas fa-edit " ></i> Edit</a>
                    <button  class="btn btn-danger"><i class="fa fa-trash" aria-hidden="true"></i>
                        Hapus
                    </button>
                </form>
                </td>
            </tr>
        <% } %>
    <% } %>
                </tbody>
    <!-- <tfoot>
        <tr>
            <th>Rendering engine</th>
            <th>Browser</th>
            <th>Platform(s)</th>
            
        </tr>
    </tfoot> -->
</table>